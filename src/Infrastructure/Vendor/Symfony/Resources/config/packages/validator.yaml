framework:
    validation:
        mapping:
            paths:
                - '%kernel.project_dir%/src/Infrastructure/Vendor/Symfony/Resources/config/validation'

when@test:
    framework:
        validation:
            not_compromised_password: false
